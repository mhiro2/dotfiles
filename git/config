[user]
  name  = mhiro2
  email = hirotsu.masaaki@gmail.com

[init]
  defaultBranch = main

[commit]
  template = ~/.config/git/commit_template

[push]
  default = current

[pull]
  ff = only

[core]
  editor = nvim
  pager = delta --plus-color="#084200" --plus-emph-color="#14a100" --minus-color="#660000" --minus-emph-color="#e00b00" --theme='Monokai Extended'

[color]
  ui = auto
  status = auto
  diff = auto
  branch = auto
  interactive = auto
  grep = auto

[pager]
  branch = cat

[diff]
  compactionHeuristic = true

[interactive]
  diffFilter = delta --color-only

[alias]
  a  = add
  b  = branch
  c  = checkout
  d  = diff
  f  = fetch
  l  = log
  lo = log --pretty=oneline
  p  = push
  r  = reset
  st = status
  ss = status -s
  sh = show

[delta]
  features = side-by-side line-numbers decorations
  whitespace-error-style = 22 reverse

[delta "decorations"]
  commit-decoration-style = bold yellow box ul
  file-style = bold yellow ul
  file-decoration-style = none

[ghq]
  root = ~/go/src
  root = ~/ghq
